# Copy to .dev.vars (gitignored) for `npx wrangler dev`. In production the same value
# is set as a Worker secret instead: npx wrangler secret put OLLAMA_API_KEY
OLLAMA_API_KEY=<apikey from ollama.com>
